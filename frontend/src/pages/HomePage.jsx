const HomePage = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center">
      <h2>React + Tailwind scaffold is up.</h2>
      <p>
        Basic bootstrapping and folder structure in place. I’ll complete the UI
        using static data first, then wire up the API.
      </p>
    </div>
  );
};

export default HomePage;